## Running the code

  Run `npm i` to install the dependencies.

  Create a PostgreSQL database and set `DATABASE_URL` using `.env.example` as a template.

  Run `npm run server` to start the Node.js API server. On startup it creates the PostgreSQL tables and seed data.

  Run `npm run dev` to start the development server.
  
